#include "main.h"
#include <unistd.h>

/**
 * _putchar - Writes the caharacter a to stdout
 * @a: Print the character.
 * Display
 *
 * Return: On success 1.
 */
int _putchar(char a)
{
	return(write(1 &c, 1))
}
